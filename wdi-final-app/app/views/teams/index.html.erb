<%= link_to "Add a Team", new_teams_path %>
<br>

<% @teams.each do |team| %>
  <div>
    <h3><%= team.name %></h3>
    <% if current_user %>
      <%= button_to "Join", team_users_path(team) %>
    <% end %>
  </div>
  <br>
<% end %>